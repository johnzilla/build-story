---
phase: 06-storyarc-adapter
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/heygen/src/index.ts
autonomous: true
requirements:
  - HGVR-05

must_haves:
  truths:
    - "adaptStoryArc is importable from '@buildstory/heygen'"
    - "HeyGenScene, AdaptOptions, AdaptResult types are importable from '@buildstory/heygen'"
    - "AdaptOptionsSchema is importable from '@buildstory/heygen'"
    - "The heygen package builds successfully with the new exports"
  artifacts:
    - path: "packages/heygen/src/index.ts"
      provides: "Public API re-exports for adapter"
      contains: "adaptStoryArc"
  key_links:
    - from: "packages/heygen/src/index.ts"
      to: "packages/heygen/src/adapter.ts"
      via: "export { adaptStoryArc } from './adapter.js'"
      pattern: "export.*adaptStoryArc.*adapter"
---

<objective>
Wire the adapter into the heygen package's public API and verify the full package builds.

Purpose: Make `adaptStoryArc`, `AdaptOptionsSchema`, and the new types (`HeyGenScene`, `AdaptOptions`, `AdaptResult`) importable from `@buildstory/heygen` so Phase 7 can consume them.

Output: Updated `index.ts` with new exports, verified build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-storyarc-adapter/06-01-SUMMARY.md

<interfaces>
<!-- Current index.ts exports (before modification) -->
From packages/heygen/src/index.ts:
```typescript
export { preflightHeyGenCheck } from './preflight.js'
export { estimateHeyGenCost } from './cost.js'
export { HeyGenOptionsSchema } from './types.js'
export type { HeyGenOptions, HeyGenConfig, HeyGenCostEstimate, PreflightResult } from './types.js'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add adapter exports to index.ts and verify package build</name>
  <files>packages/heygen/src/index.ts</files>
  <read_first>
    - packages/heygen/src/index.ts
    - packages/heygen/src/adapter.ts
    - packages/heygen/src/types.ts
  </read_first>
  <action>
Add two new export lines to `packages/heygen/src/index.ts` after the existing exports. The final file should contain all existing lines plus:

```typescript
export { adaptStoryArc } from './adapter.js'
export { AdaptOptionsSchema } from './types.js'
export type { HeyGenScene, AdaptOptions, AdaptResult } from './types.js'
```

Note: `AdaptOptionsSchema` goes on a value export line (same pattern as `HeyGenOptionsSchema` above it). The three new types go on a `export type` line (same pattern as the existing type re-exports).

After editing, run:
1. `pnpm --filter @buildstory/heygen build` -- must succeed
2. `pnpm --filter @buildstory/heygen exec vitest run` -- all tests must still pass
3. Verify the built output contains the new exports by checking the generated `.d.ts` file

Commit: `feat(06-02): export adaptStoryArc and adapter types from @buildstory/heygen`
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/build-story && pnpm --filter @buildstory/heygen build && pnpm --filter @buildstory/heygen exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - packages/heygen/src/index.ts contains `export { adaptStoryArc } from './adapter.js'`
    - packages/heygen/src/index.ts contains `export { AdaptOptionsSchema } from './types.js'`
    - packages/heygen/src/index.ts contains `export type { HeyGenScene, AdaptOptions, AdaptResult } from './types.js'`
    - packages/heygen/src/index.ts still contains all 4 original export lines (preflightHeyGenCheck, estimateHeyGenCost, HeyGenOptionsSchema, type exports)
    - `pnpm --filter @buildstory/heygen build` exits 0
    - `pnpm --filter @buildstory/heygen exec vitest run` exits 0
  </acceptance_criteria>
  <done>adaptStoryArc, AdaptOptionsSchema, HeyGenScene, AdaptOptions, and AdaptResult are all importable from '@buildstory/heygen'. Package builds and all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Re-export wiring only — no new logic, no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | I (Info Disclosure) | index.ts exports | accept | AdaptOptionsSchema does not include apiKey; no sensitive data exposed through new exports |
</threat_model>

<verification>
- `pnpm --filter @buildstory/heygen build` succeeds
- `pnpm --filter @buildstory/heygen exec vitest run` passes all tests
- `adaptStoryArc` appears in the built `.d.ts` output
</verification>

<success_criteria>
- Phase 7 can `import { adaptStoryArc } from '@buildstory/heygen'`
- Phase 7 can `import type { HeyGenScene, AdaptOptions, AdaptResult } from '@buildstory/heygen'`
- Package builds cleanly with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-storyarc-adapter/06-02-SUMMARY.md`
</output>
