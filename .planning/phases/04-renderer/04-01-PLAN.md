---
phase: 04-renderer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/types/story.ts
  - packages/core/src/types/options.ts
  - packages/core/src/narrate/prompts/system.ts
  - packages/core/src/narrate/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - NARR-10
  - NARR-11
  - NARR-12

must_haves:
  truths:
    - "'story' style produces warm third-person documentary narration with avg sentence under 15 words"
    - "StoryBeat schema accepts optional visual_cue, tone, duration_seconds fields"
    - "Narration warnings accumulate in StoryArc.metadata.warnings instead of console.warn"
    - "NarrateOptions.style union includes 'story'"
  artifacts:
    - path: "packages/core/src/narrate/prompts/system.ts"
      provides: "story style system prompt"
      contains: "story:"
    - path: "packages/core/src/types/story.ts"
      provides: "Extended StoryBeat and StoryArc schemas"
      contains: "visual_cue"
    - path: "packages/core/src/types/options.ts"
      provides: "Updated NarrateOptions with story style"
      contains: "story"
    - path: "packages/core/src/narrate/index.ts"
      provides: "Warning accumulation in metadata"
      contains: "warnings"
  key_links:
    - from: "packages/core/src/narrate/prompts/system.ts"
      to: "packages/core/src/narrate/index.ts"
      via: "buildSystemPrompt('story')"
      pattern: "STYLE_PROMPTS\\[.story.\\]"
    - from: "packages/core/src/types/story.ts"
      to: "packages/core/src/narrate/index.ts"
      via: "StoryArcSchema.parse validates metadata.warnings"
      pattern: "metadata.*warnings"
---

<objective>
Extend @buildstory/core with the "story" narrative style, StoryBeat schema extensions for video rendering, and warning accumulation in StoryArc metadata.

Purpose: These core type and prompt changes are prerequisites for the video package (Plan 02+03) and CLI wiring (Plan 04). The "story" style defines the voice for video narration. Schema extensions carry visual hints from narrator to renderer. Warning accumulation (NARR-12) keeps core pure by avoiding console.warn.

Output: Updated core types, new "story" system prompt, refactored warning handling in narrate().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-renderer/04-CONTEXT.md
@.planning/phases/04-renderer/04-RESEARCH.md

@packages/core/src/types/story.ts
@packages/core/src/types/options.ts
@packages/core/src/narrate/prompts/system.ts
@packages/core/src/narrate/index.ts
@packages/core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend StoryBeat/StoryArc schemas and NarrateOptions</name>
  <files>packages/core/src/types/story.ts, packages/core/src/types/options.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/types/story.ts, packages/core/src/types/options.ts, packages/core/src/index.ts</read_first>
  <action>
In `packages/core/src/types/story.ts`:

1. Add optional video-rendering fields to `StoryBeatSchema` (per D-24):
```typescript
visual_cue: z.string().optional(),
tone: z.string().optional(),
duration_seconds: z.number().optional(),
```

2. Add `warnings` to the `metadata` object in `StoryArcSchema`:
```typescript
metadata: z.object({
  generatedAt: z.string(),
  style: z.string(),
  sourceTimeline: z.string(),
  warnings: z.array(z.string()).optional(),
}),
```

3. Add `'remotion-script'` to `FormatTypeSchema`:
```typescript
export const FormatTypeSchema = z.enum(['outline', 'thread', 'blog', 'video-script', 'remotion-script'])
```

In `packages/core/src/types/options.ts`:

4. Add `'story'` to the `NarrateOptions.style` union:
```typescript
style: 'technical' | 'overview' | 'retrospective' | 'pitch' | 'story'
```

In `packages/core/src/index.ts`:

5. Verify all new types are re-exported. The existing exports should cover it since they export the Zod schemas and inferred types. No changes needed unless `FormatType` or `StoryBeat` type exports are missing.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/build-story && pnpm --filter @buildstory/core build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `packages/core/src/types/story.ts` contains `visual_cue: z.string().optional()`
- `packages/core/src/types/story.ts` contains `tone: z.string().optional()`
- `packages/core/src/types/story.ts` contains `duration_seconds: z.number().optional()`
- `packages/core/src/types/story.ts` contains `warnings: z.array(z.string()).optional()`
- `packages/core/src/types/story.ts` contains `'remotion-script'`
- `packages/core/src/types/options.ts` contains `'story'` in the style union
- `pnpm --filter @buildstory/core build` exits 0
  </acceptance_criteria>
  <done>StoryBeat has visual_cue/tone/duration_seconds optional fields. StoryArc.metadata has optional warnings array. NarrateOptions.style includes 'story'. FormatType includes 'remotion-script'. Core builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Add "story" system prompt and refactor warning accumulation</name>
  <files>packages/core/src/narrate/prompts/system.ts, packages/core/src/narrate/index.ts</files>
  <read_first>packages/core/src/narrate/prompts/system.ts, packages/core/src/narrate/index.ts, packages/core/src/types/story.ts</read_first>
  <action>
In `packages/core/src/narrate/prompts/system.ts`:

1. Add a `story` entry to `STYLE_PROMPTS`. The prompt must implement D-01 through D-05:
   - D-01: Warm documentary tone, friendly and informative, not trying to be funny. Think Kurzgesagt. Stakes and tension carry interest.
   - D-02: Third-person narration. "BuildStory began as a simple idea" or "John decided to rip out the ORM". NOT second-person "you".
   - D-03: Mix project-as-protagonist and developer-name. Project name for big-picture, git author name for specific decisions. Fall back to project name if no git author data.
   - D-04: Minimal humor. Warmth and personality yes, but no jokes, sarcasm, wry observations, or pop culture references.
   - D-05: Average sentence under 15 words. One idea per beat. Punchy but not breathless.

   The prompt should also instruct the LLM to populate the new optional fields:
   - `visual_cue`: a brief description of what to show on screen (e.g., "code diff scrolling", "timeline advancing", "architecture diagram")
   - `tone`: emotional register of the beat (e.g., "determined", "triumphant", "tense")
   - `duration_seconds`: suggested narration length (5-15 seconds typical)

   Include the shared `BEAT_TYPES_INSTRUCTION` and update it to document the 3 new optional fields in the JSON schema.

2. Update `BEAT_TYPES_INSTRUCTION` to include the new optional fields in the output schema:
```
"visual_cue": "<optional: brief description of what to show on screen>",
"tone": "<optional: emotional register — determined, triumphant, tense, etc.>",
"duration_seconds": <optional: suggested narration length in seconds>
```

In `packages/core/src/narrate/index.ts`:

3. Replace the two `console.warn()` calls (for hallucinated sourceEventIds) with warning accumulation into an array. Then set the array on `metadata.warnings`:

   - Create a `const warnings: string[] = []` at the top of the validation section
   - Replace `console.warn(...)` with `warnings.push(...)` 
   - Set `metadata.warnings` on the `validatedArc`:
   ```typescript
   const validatedArc: StoryArc = {
     ...finalArc,
     beats: validatedBeats,
     metadata: {
       ...finalArc.metadata,
       generatedAt: new Date().toISOString(),
       ...(warnings.length > 0 ? { warnings } : {}),
     },
   }
   ```
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/build-story && pnpm --filter @buildstory/core build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `packages/core/src/narrate/prompts/system.ts` contains a `story:` key in STYLE_PROMPTS
- The story prompt contains "third-person" or "third person"
- The story prompt contains "documentary"
- The story prompt contains "15 words" or "fifteen words"
- The story prompt does NOT contain "second-person" or "you" as a narrative instruction
- `BEAT_TYPES_INSTRUCTION` contains `visual_cue`
- `BEAT_TYPES_INSTRUCTION` contains `duration_seconds`
- `packages/core/src/narrate/index.ts` does NOT contain `console.warn`
- `packages/core/src/narrate/index.ts` contains `warnings.push(`
- `packages/core/src/narrate/index.ts` contains `metadata.warnings` or `warnings` property on validatedArc metadata
- `pnpm --filter @buildstory/core build` exits 0
  </acceptance_criteria>
  <done>"story" system prompt is in STYLE_PROMPTS with D-01 through D-05 voice rules. BEAT_TYPES_INSTRUCTION includes visual_cue, tone, duration_seconds optional fields. narrate() accumulates warnings in metadata.warnings instead of console.warn. Core builds successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → StoryArc | Untrusted JSON from LLM parsed through Zod schemas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | StoryBeatSchema | mitigate | Zod `.optional()` on new fields — invalid types rejected at parse time |
| T-04-02 | I (Info Disclosure) | warnings array | accept | Warnings contain beat titles and event IDs only — no sensitive data |
</threat_model>

<verification>
- `pnpm --filter @buildstory/core build` exits 0
- `buildSystemPrompt('story')` returns a string containing "third-person" and "documentary"
- StoryBeatSchema.parse succeeds with and without visual_cue/tone/duration_seconds
- StoryArcSchema.parse succeeds with and without metadata.warnings
- No `console.warn` calls remain in `packages/core/src/narrate/index.ts`
</verification>

<success_criteria>
- "story" style produces a system prompt with warm documentary third-person voice rules
- StoryBeat accepts optional visual_cue (string), tone (string), duration_seconds (number)
- StoryArc.metadata accepts optional warnings (string[])
- NarrateOptions.style includes 'story'
- FormatType includes 'remotion-script'
- narrate() accumulates warnings in metadata instead of console.warn
- Core package builds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-renderer/04-01-SUMMARY.md`
</output>
