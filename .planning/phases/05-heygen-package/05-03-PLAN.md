---
phase: 05-heygen-package
plan: 03
type: execute
wave: 1
depends_on: ["05-01", "05-02"]
files_modified: ["packages/cli/src/commands/render.ts"]
autonomous: true
requirements: ["REND-12"]
gap_closure: true

must_haves:
  truths:
    - "A VideoRenderer interface type is declared in packages/cli/src/commands/render.ts"
    - "The interface has readonly name, preflight, and estimateCost members matching RESEARCH.md Pattern 3"
  artifacts:
    - path: "packages/cli/src/commands/render.ts"
      provides: "VideoRenderer interface declaration"
      contains: "interface VideoRenderer"
  key_links: []
---

<objective>
Define the VideoRenderer interface inline in packages/cli/src/commands/render.ts to satisfy REND-12.

Purpose: The verification report identified that REND-12 ("Pluggable VideoRenderer interface exists so CLI can dispatch to Remotion or HeyGen by name") failed because the TypeScript interface type was never declared -- only an if/else dispatch exists. Per D-02 and RESEARCH.md Pattern 3, the interface belongs inline in render.ts (not in core, not in a shared package).

Output: Updated render.ts with VideoRenderer interface declaration. No behavioral change -- the existing if/else dispatch continues to work; this adds the type contract that REND-12 requires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-heygen-package/05-VERIFICATION.md
@packages/cli/src/commands/render.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VideoRenderer interface to render.ts</name>
  <files>packages/cli/src/commands/render.ts</files>
  <read_first>
    - packages/cli/src/commands/render.ts (current file -- understand import structure and where to place interface)
    - .planning/phases/05-heygen-package/05-RESEARCH.md (Pattern 3 -- exact interface shape)
  </read_first>
  <action>
Add the following interface declaration to `packages/cli/src/commands/render.ts`, placed after the imports and before the `renderCommand` function declaration. Import `StoryBeat` type from `@buildstory/core` (add to the existing import line that already imports `StoryArc` and `StoryArcSchema`).

The interface shape from RESEARCH.md Pattern 3 (per REND-12, Claude's discretion on interface design):

```typescript
import type { StoryArc, StoryBeat } from '@buildstory/core'

/** Pluggable renderer contract (REND-12). Inline per D-02 -- no plugin registry. */
interface VideoRenderer {
  readonly name: string
  preflight(opts: unknown): Promise<{ ok: boolean; failures: string[] }>
  estimateCost(beats: StoryBeat[]): { label: string; creditsRequired?: number; estimatedCostUSD?: number }
}
```

This is a non-exported interface -- it serves as the type contract for REND-12 but does not change runtime behavior. The existing if/else dispatch remains unchanged. Do NOT refactor the dispatch to use this interface -- that is out of scope for gap closure.

Update the existing `StoryArc` import to also include `StoryBeat`:
- Change `import type { StoryArc } from '@buildstory/core'` to `import type { StoryArc, StoryBeat } from '@buildstory/core'`

The `StoryArcSchema` import remains on the separate value-import line (it is a runtime value, not a type).
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/build-story && grep -n "interface VideoRenderer" packages/cli/src/commands/render.ts && grep -n "preflight" packages/cli/src/commands/render.ts | head -3 && grep -n "estimateCost" packages/cli/src/commands/render.ts | head -3 && grep -n "StoryBeat" packages/cli/src/commands/render.ts | head -3 && pnpm --filter buildstory build</automated>
  </verify>
  <acceptance_criteria>
    - grep "interface VideoRenderer" packages/cli/src/commands/render.ts returns exactly 1 match
    - grep "readonly name: string" packages/cli/src/commands/render.ts returns exactly 1 match
    - grep "preflight(opts: unknown)" packages/cli/src/commands/render.ts returns at least 1 match
    - grep "estimateCost(beats: StoryBeat" packages/cli/src/commands/render.ts returns at least 1 match
    - grep "import type.*StoryBeat.*from '@buildstory/core'" packages/cli/src/commands/render.ts returns 1 match
    - pnpm --filter buildstory build exits 0
  </acceptance_criteria>
  <done>VideoRenderer interface is declared in render.ts with name, preflight, and estimateCost members matching RESEARCH.md Pattern 3. CLI builds successfully. REND-12 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This change adds a type declaration only -- no runtime behavior change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | N/A | render.ts interface | accept | Type-only change; no runtime effect; no new inputs or outputs |
</threat_model>

<verification>
1. `grep "interface VideoRenderer" packages/cli/src/commands/render.ts` returns a match
2. `pnpm --filter buildstory build` exits 0 (CLI compiles with the new type import)
3. The existing if/else dispatch is unchanged (no behavioral regression)
</verification>

<success_criteria>
- REND-12 gap closed: VideoRenderer interface exists in packages/cli/src/commands/render.ts
- Interface has readonly name, preflight(opts), and estimateCost(beats) members
- CLI builds without errors
- No runtime behavior change from Phase 5 plans 01-02
</success_criteria>

<output>
After completion, create `.planning/phases/05-heygen-package/05-03-SUMMARY.md`
</output>
