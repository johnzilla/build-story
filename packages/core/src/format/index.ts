import type { StoryArc } from '../types/story.js'
import type { FormatType } from '../types/story.js'

export async function format(arc: StoryArc, formatType: FormatType): Promise<string> {
  // Stub: converts StoryArc to text — real rendering added in format phase
  void arc
  void formatType
  return ''
}
